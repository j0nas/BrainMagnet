<h2>Sign up</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

    <p><div class="field input-group">
  <span class="input-group-addon"><%= f.label :name %></span>
  <%= f.text_field :name, class:"form-control"%>
</div></p>

    <p><div class="input-group">
  <span class="input-group-addon"><%= f.label :email %></span>
  <%= f.email_field :email, class:"form-control" %>
</div></p>

    <p><div class="input-group">
  <span class="input-group-addon"><%= f.label :password %></span>
  <%= f.password_field :password, class:"form-control",  autocomplete: "off", placeholder:"Leave blank if you don't want to change it" %>
  <% if @validatable %>
      <span class="input-group-addon"><%= @minimum_password_length %> characters minimum</span>
  <% end %>
</div></p>


    <p><div class="input-group">
  <span class="input-group-addon"><%= f.label :password_confirmation %></span>
  <%= f.password_field :password_confirmation, class:"form-control", autocomplete: "off", placeholder:"Type your new password again. Just making sure." %>
</div></p>

    <p><input type="submit" name="commit" value="Sign up" class="btn btn-default"></p>
<% end %>

<%= render "devise/shared/links" %>
